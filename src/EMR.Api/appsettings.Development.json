{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_comment": "Development-only settings. These override appsettings.json for local development.",
  "_security_note": "NEVER commit actual credentials. Use environment variables or user-secrets.",
  "ConnectionStrings": {
    "_instructions": "Docker container connection strings for local development",
    "DefaultConnection": "Host=localhost;Port=5432;Database=emr_db;Username=postgres;Password=postgres",
    "Redis": "localhost:6379",
    "MongoDB": "mongodb://localhost:27017/emr_db"
  },
  "KeyVault": {
    "Url": ""
  }
}
